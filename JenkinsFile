pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Clone source successfully'
            }
        }

        stage('Build') {
            steps {
                bat 'dotnet --version'
            }
        }
    }
}